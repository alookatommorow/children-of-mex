language: ruby
rvm: 2.2.3
test:
  adapter: postgresql
  database: children-of-mex_test
  username: postgres
before_script:
- bundle exec rake db:create RAILS_ENV=test
- bundle exec rake db:migrate RAILS_ENV=test
script:
- RAILS_ENV=test xvfb-run -a bundle exec rake spec
